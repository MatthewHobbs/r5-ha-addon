# Local-only upstream reference (Topolino65/renault-5-dashboard-view) — sourced for
# assets, never committed.
reference/

# macOS / Python / tooling
.DS_Store
._*
__pycache__/
*.py[cod]
.ruff_cache/
.venv/
venv/

# Secrets / local env (the add-on takes credentials via its config page)
secrets.yaml
.env
*.local
