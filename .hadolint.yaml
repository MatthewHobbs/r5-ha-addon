ignored:
  - DL3013   # pin pip versions (we use a requirements.txt with version floors)
  - DL3018   # pin apk versions
  - DL3025   # HEALTHCHECK CMD uses shell form for the `|| exit 1` fallback (not expressible in JSON exec form); flagged by hadolint v3.4.0
