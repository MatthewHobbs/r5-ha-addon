{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "Renovate covers what Dependabot cannot see in this repo: the CORE_REF git-SHA pin in the Dockerfile, which no manifest describes, and the Dockerfile base and build-stage images.",
    "Python is deliberately left to Dependabot. requirements.txt is a hash-locked pip-compile output installed with --require-hashes, and it carries no pip-compile header, so Renovate cannot regenerate the hashes and would leave the lock inconsistent.",
    "renault-api stays manual regardless of tooling: per-model endpoint support is hard-coded in the library, so it is bumped by hand after a container-verified review.",
    "It also tracks the UI gate's render inputs in ui-tests/run.sh - the Home Assistant image and the four custom cards - which are pinned for a deterministic render, so without a bot they only move when someone remembers, and the gate drifts behind the Home Assistant users actually run."
  ],
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommitTypeAll(build)"
  ],
  "enabledManagers": [
    "github-actions",
    "dockerfile",
    "custom.regex"
  ],
  "timezone": "Europe/London",
  "schedule": [
    "before 6am on saturday"
  ],
  "semanticCommits": "enabled",
  "semanticCommitType": "build",
  "semanticCommitScope": "deps",
  "labels": [
    "dependencies"
  ],
  "prConcurrentLimit": 3,
  "packageRules": [
    {
      "description": "One PR for all GitHub Actions, matching the grouping Dependabot used before this change.",
      "matchManagers": [
        "github-actions"
      ],
      "groupName": "actions"
    },
    {
      "description": "Base and build-stage images move independently of the add-on, so keep them in their own PR to be container-verified on their own.",
      "matchManagers": [
        "dockerfile"
      ],
      "groupName": "container images"
    },
    {
      "description": "The shared core is a runtime-code change, never a routine bump. Keep it on its own PR so the build-and-boot gate applies to it alone.",
      "matchManagers": [
        "custom.regex"
      ],
      "groupName": "renault-mqtt core",
      "commitMessageTopic": "the renault-mqtt core",
      "matchDepNames": [
        "MatthewHobbs/renault-mqtt"
      ]
    },
    {
      "description": "The UI gate's render inputs move together, as ui-tests/run.sh requires: one PR carrying the Home Assistant image and all four cards, so the gate on that PR confirms the exact combination it will then pin. groupSingleUpdates keeps a lone bump (e.g. only a new Home Assistant) on that same group branch rather than a per-dependency PR; Renovate's default does not apply group settings to a single update. separateMajorMinor is off for the same reason: a major (a new year for Home Assistant, a card's major version) would otherwise split into a second PR and the gate would never see the combination. It runs at any time rather than the repo's Saturday schedule: the gate's stable leg must track current stable (global rule), so a new Home Assistant release should reach a PR within hours, not up to a week.",
      "matchManagers": [
        "custom.regex"
      ],
      "matchDepNames": [
        "ghcr.io/home-assistant/home-assistant",
        "piitaya/lovelace-mushroom",
        "custom-cards/button-card",
        "thomasloven/lovelace-card-mod",
        "Clooos/Bubble-Card"
      ],
      "groupName": "UI gate render inputs",
      "commitMessageTopic": "the UI gate's Home Assistant and card pins",
      "groupSingleUpdates": true,
      "separateMajorMinor": false,
      "schedule": [
        "at any time"
      ]
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": [
        "CORE_REF pins the shared renault-mqtt core by immutable commit SHA. No manifest declares it, so Dependabot cannot see it at all and it has only ever been bumped by hand.",
        "The version comment and the SHA are captured together and rewritten as a pair, so the comment cannot drift away from the pin beneath it. This is the same shape as a SHA-pinned GitHub Action carrying a version comment.",
        "Tag datasource, not the branch head: a bump should mean a released core, not merely the newest commit on main. If a release ever ships untagged, Renovate goes quiet rather than proposing a bump - the safe direction to fail."
      ],
      "managerFilePatterns": [
        "/(^|/)Dockerfile$/"
      ],
      "matchStrings": [
        "# renault-mqtt (?<currentValue>v[\\d.]+)\nARG CORE_REF=(?<currentDigest>[a-f0-9]{40})"
      ],
      "autoReplaceStringTemplate": "# renault-mqtt {{{newValue}}}\nARG CORE_REF={{{newDigest}}}",
      "depNameTemplate": "MatthewHobbs/renault-mqtt",
      "datasourceTemplate": "github-tags",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": [
        "The Home Assistant image the UI gate renders on. Stable releases only: the regex versioning rejects beta (2026.10.0b1), dev and the floating stable/latest tags, which made the gate flake before it was pinned. Home Assistant's monthly release is the version's minor here."
      ],
      "managerFilePatterns": [
        "/(^|/)ui-tests/run\\.sh$/"
      ],
      "matchStrings": [
        "HA_IMAGE=\"\\$\\{HA_IMAGE:-(?<depName>ghcr\\.io/home-assistant/home-assistant):(?<currentValue>\\d+\\.\\d+\\.\\d+)\\}\""
      ],
      "datasourceTemplate": "docker",
      "versioningTemplate": "regex:^(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)$"
    },
    {
      "customType": "regex",
      "description": [
        "Cards the UI gate downloads as GitHub release assets (mushroom, button-card).",
        "Strict vX.Y.Z only: card-mod still carries bare legacy tags (3 ... 14, 3.3.1, v0.2, beta), and semver-coerced read '14' as 14.0.0 - newer than v4.2.1 - so Renovate queued card-mod v4.2.1 -> 14. The strict pattern also drops -beta/-rc/-dev tags."
      ],
      "managerFilePatterns": [
        "/(^|/)ui-tests/run\\.sh$/"
      ],
      "matchStrings": [
        "https://github\\.com/(?<depName>[^/\\s]+/[^/\\s]+)/releases/download/(?<currentValue>v\\d+\\.\\d+\\.\\d+)/"
      ],
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "regex:^v(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)$"
    },
    {
      "customType": "regex",
      "description": [
        "Cards the UI gate fetches from jsDelivr at a git tag (card-mod, Bubble Card). jsDelivr serves the tag, so track tags.",
        "Strict vX.Y.Z only: card-mod still carries bare legacy tags (3 ... 14, 3.3.1, v0.2, beta), and semver-coerced read '14' as 14.0.0 - newer than v4.2.1 - so Renovate queued card-mod v4.2.1 -> 14. The strict pattern also drops -beta/-rc/-dev tags."
      ],
      "managerFilePatterns": [
        "/(^|/)ui-tests/run\\.sh$/"
      ],
      "matchStrings": [
        "https://cdn\\.jsdelivr\\.net/gh/(?<depName>[^/@\\s]+/[^/@\\s]+)@(?<currentValue>v\\d+\\.\\d+\\.\\d+)/"
      ],
      "datasourceTemplate": "github-tags",
      "versioningTemplate": "regex:^v(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)$"
    }
  ]
}
